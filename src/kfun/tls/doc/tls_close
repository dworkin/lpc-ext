NAME
	tls_close - close a TLS session

SYNOPSIS
	string tls_close()


DESCRIPTION
	Close the TLS session that is associated with the current object.  This
	will deallocate the internal TLS context.  The returned string, if not
	nil, will tell the other side to shut down the TLS session.

SEE ALSO
	kfun/tls_client, kfun/tls_receive, kfun/tls_send, kfun/tls_server
